{
  "name": "violet-daily-digest-prototype",
  "version": "0.1.0",
  "private": true,
  "description": "Discord archive viewer, important messages, persistent pins and grounded AI retrieval.",
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=20.12"
  }
}
